Below is a **ChatGPT prompt** designed to produce a 15+ page detailed code document that improves the **user interface** of the `logos_heraclitus_cosmic_principle` repository.

> Copy the entire prompt block below, paste it into a fresh ChatGPT conversation, and execute. The assistant will follow all instructions to deliver the final output.

```markdown
# IMPROVING THE USER INTERFACE OF LOGOS HERACLITUS COSMIC PRINCIPLE

You are an expert in **frontend development, blockchain UX, and privacy‑preserving decentralised applications**. Your task is to produce a **detailed, code‑first technical document** (minimum 15 pages in print) that **improves the user interface** of the Logos Heraclitus Cosmic Principle repository:  
[https://github.com/lucylow/logos_heraclitus_cosmic_principle](https://github.com/lucylow/logos_heraclitus_cosmic_principle)

The current frontend is **minimal or scattered**. The goal is to **design and implement a complete, modern, user‑friendly web application** that interacts with the running Logos node via RPC / WebSocket, and that exposes the unique features of **Cryptarchia (Private Proof of Stake)** and **Blend (Proposer Anonymity)** to end users in an intuitive way.

---

## DELIVERABLES

Produce a **single Markdown document** with the following sections:

1. **Executive Summary** (max 2 pages)  
   - Current state of the frontend (what exists, what is missing).  
   - High‑level vision for the improved UI.  
   - List of **five core features** you will implement.

2. **Architecture & Technology Stack** (2–3 pages)  
   - Framework: **Next.js 14** (App Router) with TypeScript.  
   - Styling: **Tailwind CSS** + **shadcn/ui** component library.  
   - State management: **Zustand**.  
   - Communication with the Logos node: **WebSockets (JSON‑RPC 2.0)** for real‑time updates, plus HTTP fallback.  
   - Wallet connection: **Logos Basecamp SDK** or custom key manager (as described in the repository).  
   - Decoding of on‑chain data: **SPEL** framework (fetch & decode account data).  
   - Provide a complete **dependency graph** and a **folder structure**.

3. **Detailed Code & Explanations** (13+ pages)  
   For **each** of the five core features below, provide:  
   - Rationale (2–3 sentences).  
   - **Complete, runnable code** (React components, hooks, TypeScript types, API routes).  
   - Integration instructions (how to plug into the existing repository’s build system).  
   - A screenshot / ASCII mockup of the UI element.

   **Core features (please implement all):**

   - **A. Dashboard** – real‑time node & network overview.  
     - Live display: current block height, active validators (anonymised – show only their blinded identities), network participation (uptime, slot success rate), pending transaction count.  
     - Use WebSocket to subscribe to `chain`, `consensus` and `blendstats` events.  
     - Provide a “status card” component that turns from green (healthy) to yellow (degraded) to red (offline).  
     - Show the current **lottery difficulty** (without revealing individual stake).  
     - **Code**: `components/Dashboard.tsx`, `components/StatusCard.tsx`, `hooks/useNodeStatus.ts`, `hooks/useWebSocket.ts`, API route `/api/node/status`.

   - **B. Privacy Staking & Delegation Interface** – shielded stake creation and delegation.  
     - A form that allows users to create a shielded note (stake) using the existing zk‑based note system.  
     - Display commitment hash and nullifier.  
     - Show the user’s current active notes (blinded), without revealing their exact stake.  
     - Provide a “delegate” flow where a user can assign voting rights to another blinded note.  
     - All transactions must be built using the SDK and sent via the local node.  
     - **Code**: `components/StakeManager.tsx`, `components/NoteViewer.tsx`, `lib/cryptarchia/stake.ts`, wallet signing modal.

   - **C. Blend Anonymity Health Monitor** – visualise the mixnet and cover traffic.  
     - A real‑time graph showing the **onion‑encrypted path** of a dummy block proposal (without revealing the real proposer).  
     - Display: number of active mix nodes, current cover traffic rate (dummy messages per second), estimated anonymity set size.  
     - A “timing obfuscation” visualiser that shows random delays as a histogram.  
     - Use WebSocket subscriptions to `/api/blend/cover_traffic` and `/api/blend/anonymity_stats`.  
     - **Code**: `components/BlendMonitor.tsx`, `components/CoverTrafficChart.tsx`, `hooks/useBlendStats.ts`.

   - **D. Block Proposal & Lottery Viewer** – transparent yet anonymous block production.  
     - Animate the local leadership lottery process (VRF, difficulty, proof generation).  
     - For each new slot, display whether a block was proposed, but **never reveal the proposer’s identity**.  
     - Show the **lottery ticket** (hash of VRF output) without linking it to a validator.  
     - Include a “Proof of Leadership” verification viewer – paste a proof and see if it would have won a slot.  
     - **Code**: `components/LotteryViewer.tsx`, `components/ProposalTimeline.tsx`, `lib/cryptarchia/lottery.ts`, WebSocket for new slots.

   - **E. Transaction Builder & Private Transfer** – shielded token transfers using LEZ.  
     - A multi‑step form: select input notes, specify recipient (blinded address), amount.  
     - Generate the required zero‑knowledge proof client‑side (with a progress indicator).  
     - Broadcast the transaction via the Blend mixnet (i.e., use the `BlendClient` to anonymise).  
     - Display final confirmation and link to the transaction receipt (which may be delayed due to mixnet latency).  
     - **Code**: `components/TransferForm.tsx`, `components/ProofProgress.tsx`, `lib/transactions/builder.ts`, `lib/blend/client.ts`.

4. **Integration Checklist** (1 page)  
   - Step‑by‑step instructions to integrate the new UI into the existing repository:  
     - Add `frontend/` directory.  
     - Install dependencies (`package.json`).  
     - Configure environment variables (RPC endpoints, WebSocket URL, SPELbook URL).  
     - Run `npm run dev` alongside the Logos node.  
     - Build for production (`npm run build`).  
   - List of required environment variables and their default values.

5. **Performance & UX Benchmarks** (1 page)  
   - Metrics: Time to First Paint (TTFP), Time to Interactive (TTI), WebSocket reconnection latency, proof generation feedback delay.  
   - Provide a **benchmark script** (bash / Node) that simulates 10 concurrent users and reports frontend responsiveness.  
   - Show expected results (e.g., “90% of interactions under 200ms”).

6. **Security & Privacy Considerations** (1 page)  
   - How the frontend handles private keys (they never leave the browser; stored in session storage encrypted).  
   - Protection against XSS, CSRF, and network‑level correlation attacks (e.g., loading all assets locally, no third‑party fonts).  
   - How the UI avoids leaking metadata that could de‑anonymise a proposer (no log messages containing node IDs, no timing patterns that correlate with activity).

---

## TECHNICAL CONSTRAINTS

- **All code must be runnable** – no placeholders like `…` (except for very long repetitive blocks such as extensive CSS).  
- **Use modern React patterns** – functional components, hooks, context providers.  
- **Communicate with the Logos node** via the existing RPC interface. Assume it exposes:  
  - HTTP JSON‑RPC on `http://localhost:8545`  
  - WebSocket on `ws://localhost:8546`  
  - A `blend` module that can be queried for stats and used to broadcast anonymised transactions.  
- **Type Safety** – Provide full TypeScript definitions for all RPC responses, especially for Cryptarchia‑specific messages (e.g., `SlotInfo`, `LotteryTicket`, `ProofOfLeadership`).  
- **Use the SPEL framework** to decode on‑chain account data – show an example of calling `spel inspect` from the UI to display a validator’s note (blinded).  
- **Local Development** – Include a `docker-compose.frontend.yml` that runs a mock node for testing (simulates block production, cover traffic, and lottery events).  
- **No external analytics or tracking scripts** – the UI must respect Logos’s privacy ethos.

---

## EXAMPLE CODE BLOCK (to set the standard)

```tsx
// components/Dashboard.tsx
"use client";
import { useNodeStatus } from "@/hooks/useNodeStatus";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export function Dashboard() {
  const { data, isLoading } = useNodeStatus(3000); // refresh every 3s
  if (isLoading) return <div>Connecting to node…</div>;
  return (
    <div className="grid gap-4 md:grid-cols-2 lg:grid-cols-3">
      <Card>
        <CardHeader><CardTitle>Current Block</CardTitle></CardHeader>
        <CardContent>{data?.blockHeight}</CardContent>
      </Card>
      <Card>
        <CardHeader><CardTitle>Active Validators</CardTitle></CardHeader>
        <CardContent>{data?.activeValidators}</CardContent>
      </Card>
      <Card>
        <CardHeader><CardTitle>Pending Transactions</CardTitle></CardHeader>
        <CardContent>{data?.pendingTxs}</CardContent>
      </Card>
    </div>
  );
}
```

---

Now, write the document. Ensure the final output is at least **15 pages** (approximately 5000–7000 words of text plus code). Use clear Markdown headings, code fences, and bullet points. Make sure every code block is **complete** and **directly usable** in a Next.js project.
```

