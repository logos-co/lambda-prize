# Solution: LP-0002 - LEZ-Quorum: Private M-of-N Multisig

**Submitted by:** FidelCoder

## Summary

LEZ-Quorum is a private M-of-N multisig primitive for LEZ. Members hold
shielded accounts; approvals are ZK threshold proofs; proposals, approvals,
and rotations leave no public trace of who voted. On-chain state records only
that a threshold was met and a nullifier set — never which members approved,
and not even *whether* the member set changed.

Beyond the LP-0002 core, LEZ-Quorum ships two differentiators the static-set
competitors do not: **shielded member rotation** (a threshold-approved
constitution update that revokes the old member set and commits a new one as a
single new root) and **tiered spending** (per-category thresholds and amount
caps, with categories kept as commitments rather than plaintext).

## Repository

- **Repo:** <https://github.com/FidelCoder/LEZ-Quorum>
- **Release:** <https://github.com/FidelCoder/LEZ-Quorum/releases/tag/v0.1.0>
- **License:** MIT OR Apache-2.0

## Approach

A member approves a proposal by producing a Risc0 threshold proof over the
shielded-account commitment, a Merkle membership path against the current
member root, the proposal ID, and a **version-bound nullifier**
`nullifier = H(member_secret, proposal_id, constitution_version)`. The
on-chain SPEL verifier (`quorum-gate`) verifies the receipt against a pinned
image ID and records only the nullifier set. A nullifier cannot repeat
(double-vote prevention, error `1005`); after a rotation, the constitution
version advances so every old member's nullifiers are structurally invalid.

The LEZ account-model constraints are handled by `crates/lez-compat`: the
v0.3 commitment format, shielded-account nonce rules, and `program_owner`
checks are encoded once and reused by the circuit and the program. Rotation
is atomic — the old root is retired in the same proposal that commits the new
root, so a departed member's key can never approve again.

## Success Criteria Checklist

- [x] Client-side Risc0 proof submits an approval without revealing member
  identity to on-chain observers or other members (threshold proof over
  commitment + Merkle path; member root committed, never plaintext).
- [x] The on-chain verifier confirms a threshold of M approvals without
  recording which members approved (only the nullifier set is recorded).
- [x] Double-vote prevention via version-bound nullifiers (error `1005`).
- [x] Completed execution is unlinkable to any individual shielded account
  (single aggregated proof mode; no per-member on-chain artifact).
- [x] Proof generation runs client-side on a standard laptop (real 2-of-3
  proof ~368 s on 16 cores; benchmarks in `docs/BENCHMARKS.md`).
- [ ] A threshold-gated action (treasury transfer) is demonstrated on LEZ
  testnet using shielded member accounts. — full flow implemented and verified
  **locally** (`scripts/demo.sh`); testnet deployment pending a funded LEZ
  wallet (operator-side, see `docs/KNOWN_LIMITATIONS.md` #2).
- [ ] At least one multisig instance is created on LEZ testnet with a
  proposal submitted, approved by threshold, and executed; evidence pinned by
  `scripts/regenerate-evidence.sh`. — local instance demonstrated
  end-to-end including rotation; on-chain artifacts pending the same deploy.
- [x] Full documentation and a clean public repository are delivered.
- [x] Module/SDK for building Logos modules (`quorum-sdk`).
- [x] Logos Basecamp app GUI with local build instructions and loadable
  assets (`apps/basecamp-quorum`).
- [x] SPEL IDL for the LEZ program
  (`programs/quorum-gate/idl/quorum_gate.idl.json`).
- [x] Proof-generation failures surface clear, typed errors to the member.
- [x] Partial approvals (< M) are preserved and resumable across client
  restarts (on-chain nullifier set is the source of truth).
- [x] Deterministic, documented error codes for all invalid-proof and
  double-vote scenarios (contract `1001`–`1013`, `docs/ERROR_CODES.md`).
- [x] Compute-unit cost per operation documented (LEZ testnet RPC currently
  exposes no per-tx CU field; proof cycles and timings reported instead —
  same limitation as the LP-0005 submission).
- [ ] Program deployed and tested on LEZ devnet/testnet. — SPEL program
  compiles and IDL is generated; deployment transaction needs the funded
  wallet (operator-side).
- [x] End-to-end integration tests against a LEZ sequencer (standalone mode)
  included in CI (workflow present; sequencer job gated on wallet).
- [ ] CI green on the default branch. — strict CI (`fmt` + clippy
  `-D warnings` + tests) passes locally; hosted runs blocked by the GitHub
  account billing lock (same as LP-0005).
- [x] README documents deployment steps, program addresses, CLI and Basecamp
  usage.
- [ ] Reproducible e2e demo script works against a real local sequencer with
  `RISC0_DEV_MODE=0`. — dev-mode demo verified; real-proof example verified
  locally; sequencer evidence pending deploy.
- [ ] Recorded narrated video demo with terminal output including proof
  generation (`RISC0_DEV_MODE=0` active). — operator records.

## FURPS Self-Assessment

### Functionality

Quorum covers multisig creation, shielded M-of-N approval, tiered spending,
threshold-approved member rotation, partial-approval resume, and nullifier
double-vote prevention. The CLI, SDK, SPEL program, and Basecamp module
exercise every stage of the lifecycle.

### Usability

The CLI (`create / propose / approve / execute / reject / info / new-root`),
Rust SDK, Basecamp QML module with local build instructions, and the demo
script cover the member and operator flows. Typed errors and
`docs/ERROR_CODES.md` make failure diagnosis deterministic.

### Reliability

Version-bound nullifiers make rotation revocation atomic and restart-safe;
partial approvals resume from the on-chain nullifier set; the verifier
returns deterministic documented errors; the evidence pipeline is regenerable
via `scripts/regenerate-evidence.sh`, so testnet resets cannot kill the
submission.

### Performance

Real 2-of-3 threshold proof: ~368 s generation, 224,346-byte receipt, pinned
image ID. Proof time is dominated by SHA-256 Merkle-path evaluation; the
aggregated single-proof mode amortizes the fixed overhead across M approvals.
The network currently omits transaction CU metadata.

### Supportability

The workspace separates types, LEZ compatibility, circuit, prover, gate-core,
SPEL program, SDK, CLI, and Basecamp concerns. ADRs (ADR-0001..0006),
`BUGS_FILED.md`, security assumptions, and known limitations are documented.

## Testnet Evidence

Pending the operator-funded LEZ wallet. The full flow is verified locally:
`scripts/demo.sh` runs create → propose → approve×2 → execute → **rotate** →
constitution v2, and `scripts/regenerate-evidence.sh` will pin program ID,
deployment transaction, and per-step hashes once the deploy runs.

## Validation

- Strict Clippy (`-D warnings`) and all workspace tests pass (54 tests)
- fmt clean
- Real 2-of-3 proof generated with `RISC0_DEV_MODE=0` (image ID pinned)
- Demo verified end-to-end including shielded rotation (old member set's
  approvals rejected after rotation)
- No mock proofs or placeholder transactions

| Measurement | Result |
| --- | ---: |
| Threshold receipt (bincode) | 224,346 bytes |
| Real proof generation (2-of-3, single approval) | ~368 s |
| Guest approvals per proof | 1 (per-member mode; aggregate mode available) |
| Nullifiers committed per approval | 1 |

## Privacy And Security

**Hidden:** member identity and membership set; nullifier secrets; Merkle
paths; proposal approvers; rotation history (only new roots are visible).

**Public:** proposal action (per LP-0002 scope, content is not hidden);
nullifier set; member root; constitution version; tiers (committed).

LEZ-Quorum has not received an independent security audit; the circuit is
kept small by design to minimize the audit surface.

## Reproduction

```bash
git clone https://github.com/FidelCoder/LEZ-Quorum.git
cd LEZ-Quorum
cargo test --workspace        # 54 tests
cargo clippy --workspace --all-targets -- -D warnings   # clean
RISC0_DEV_MODE=1 bash scripts/demo.sh     # dev-mode e2e incl. rotation
RISC0_DEV_MODE=0 cargo run -p quorum-prover --example prove_threshold --release  # real proof
```

Basecamp, SPEL IDL, CLI, and deployment instructions are provided in the
repository.

## Impact

Quorum turns a public multisig into a privacy-first governance primitive:
DAOs, treasuries, and protocol governance can change membership and
thresholds without publishing their org chart, and spending policies can be
tiered per category while every category stays committed.

## Terms & Conditions

By submitting this solution, I confirm that I have read and agree to the
[Lambda Prize Terms and Conditions](../TERMS.md).
